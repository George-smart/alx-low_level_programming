#include <stdlib.h>
#include "main.h"
/**
 * _abs - check for absolute value
 *@i: check for absolute of i
 * Description: check for absolute value
 * Return: absolute value
 */
int _abs(int i)
{
	int c = abs(i);

	_putchar(c);
	return (0);
}
