#include <stdio.h>
/**
 * main - entry point
 *
 * Description: Print the lowercase of a-z
 *
 * Return: zero
 */
int main(void)
{
	char ch;

	for (ch = 'a'; ch <= 'z'; ch++)
		putchar(ch'\n');
	return (0);
}
